---
week: 1
dates: 2026-04-20 to 2026-04-26
sprint_week: 1 of 5
sprint_end: 2026-05-18
---

# Week 1 Worklog — CrossSystemEval

## Goal for this week

Establish the intellectual and infrastructural foundation before building scenarios. Deliverables by Sunday 4/26:

1. Research agenda document (2-3 pages, publication-quality framing)
2. Targeted annotated bibliography (gap-oriented, not comprehensive)
3. TRIDENT replication harness scaffolded and ready to run
4. Week 1 diary of decisions and open questions

## Context going into Week 1

- BlueDot Technical AI Safety Project Sprint accepted.
- First cohort meeting completed; most participants are writing research agendas and reproducing baselines.
- Scope expanded in Week 0 conversation to include role-attribute analysis (power dynamics via French & Raven, prosocial intent clarity) — significantly raising theoretical ambition.
- Core methodological move: pairwise cross-role consistency as primary metric; Inappropriate Convergence Rate (ICR) as headline.

## Decisions made

### Scope: NY Kendra's Law over California LPS
Kendra's Law (MHL §9.60) introduces Assisted Outpatient Treatment as a fourth legal pathway, creating richer divergence structure for judge and social worker roles. LPS is more uniformly inpatient-oriented.

### Methodology: research-first, not build-first
Shifted after reflecting that scope expansion introduces new theory that needs prior-art check; cohort norm is research agenda + baseline reproduction; deviating risks legibility; TRIDENT replication validates the scoring pipeline before investing in novel scenarios.

### Pre-registered hypotheses
- **H1**: ICR > 0 across all tested models (standard bleed is measurable).
- **H2**: ICR discriminant from sycophancy (Pearson r < 0.8 threshold pre-registered).
- **H3 (exploratory)**: ICR varies by jurisdiction, role-pair ranking invariant.
- **H4 (exploratory)**: Domain-specialized models exhibit higher ICR than base counterparts.
- **H5 (exploratory)**: Power-asymmetric bleed — bleed higher toward high-power roles.
- **H6 (exploratory)**: Altruism-asymmetric sycophancy — sycophancy higher toward high-prosocial-clarity roles.

### Model count (revised Wed 4/22 → updated to 6 primary + 2 exploratory)
Initial plan was 3 models. Revised upward after recognizing that claims of the form "H1 holds across all tested models" need a factorial provider × size design to be credible. Comparable papers: TRIDENT=19, ELEPHANT=11, SycoEval-EM=20, PAS=3.

**Primary (6 models, factorial provider × size):**
- Anthropic: Claude 3.5 Sonnet + Claude 3.5 Haiku
- OpenAI: GPT-4o + GPT-4o-mini
- Google: Gemini 2.5 Flash
- Meta: Llama 3.3 70B

**Exploratory (H4 — domain-specialization):**
- Meditron-70B vs. Llama 3.3 70B (base)
- Saul-7B-Instruct vs. Llama 3.1 7B (if time permits)

Budget: 3 scenarios × 6 roles × 6 models × 2 judges ≈ 216 primary judge calls + 216 sycophancy-probe judge calls = ~432 API calls. $5-22 total. Fits the sprint.

## Parallel work kicked off (Monday 4/20)

Spun up 5 background subagents:
1. ✅ Lit review: authority/expertise bias in LLMs — 10 papers, 8.6KB
2. ✅ Lit review: role-based / persona evaluation — 10 papers, 7.6KB
3. ✅ Lit review: street-level bureaucracy × AI — 10 papers, 6.5KB
4. ✅ Lit review: mental health AI evals — 10 papers, 7.2KB
5. ✅ Dev: TRIDENT replication harness — complete
   - First attempt (Mon 4/20): blocked on write permissions.
   - Second attempt (Wed 4/22): wrote 13 of 16 files before hitting API quota.
   - Third pass (Wed 4/22 pm, inline): finished `tests/__init__.py`, `tests/conftest.py` (FakeChatClient + tiny_dataset_dir fixture), `tests/test_loader.py` (14 tests), `tests/test_judge.py` (11 tests), `tests/test_pipeline.py` (5 tests), `notebooks/calibration_check.ipynb` (8 cells: load → aggregate → bar chart vs published → κ → pass/fail verdict). All Python syntax-valid; notebook JSON-valid. Ready to run.

## Findings from literature integration (Tue 4/21 – Wed 4/22)

### The novelty claim holds — qualified

Each of the four reviews produced a near-miss paper that would have invalidated CrossSystemEval had it existed:
- **Wagner et al. (2025, JAMA)** — mandated-reporter role-framing variation. Closest methodological precedent. Cite prominently, differentiate on (a) commitment vs. reporting, (b) explicit divergence matrix.
- **U-SafeBench (2025)** — same cell structure (query × user × expected response) but user profiles are vulnerabilities, not professional roles, single domain.
- **Ramirez et al. (2025)** — civil commitment + competency benchmark, single forensic-evaluator role, no cross-role variation.
- **Overalignment in Healthcare (Bhatia et al., 2026)** — expert-nudge vs. basic-nudge, single domain, no French & Raven taxonomy.

### Two new risks surfaced

- **Liang et al. (Oct 2025, 2510.24677)** — mechanistically show models don't internally represent user authority; role pathways nearly identical, surface language changes only. Null-hypothesis threat for H5.
- **Chen et al. (Aug 2025)** — role-play fidelity study showed models **sanitize danger cues** even when explicitly prompted. Safety-training artifact could undermine commitment-scenario fidelity.

### Lipsky framing defensible with precision
"First application of Lipsky to AI" is taken (Alkhatib & Bernstein 2019, Bullock 2019, Vredenburgh 2023). **"First computational benchmark operationalizing Lipsky's framework for LLM role-appropriate discretion"** is defensible.

## UI shipped (Wed 4/22)

Research dashboard at `/ui/` for navigating findings:
- Next.js 14 App Router + Tailwind + custom components (no shadcn — tighter design control)
- Pages: Overview, Research Agenda, Literature (4 reviews), Worklog, TRIDENT

### Dark mode (Wed 4/22 pm)
- Switched from light palette to Apple-inspired dark system (#0a0a0c surface, #f5f5f7 ink, #0a84ff accent)
- `color-scheme: dark` on `:root`; ambient radial-gradient backdrop at 6%/3.5% opacity for subtle depth
- Cards: swapped drop shadow for inset hairline ring + soft shadow (shadows don't register on dark)
- Sidebar active state: `bg-surface-muted` with accent-blue icon (light-mode ink pill would be white-on-white)

## Open questions to raise with mentor

1. Is adding power/altruism role-attribute coding too much scope for Phase 1, or the right move to make findings explanatory rather than descriptive?
2. For H2 discriminant validity: is r < 0.5 (clear) vs. r < 0.8 (acceptable) the right threshold, or should pre-register more conservatively?
3. Is the divergence matrix structure defensible if constructed solo, or do we need at least one domain expert validation before Week 4?
4. Given Chen et al.'s finding that models sanitize danger cues in role-play, should fact patterns include adversarial red-team variants from the start?
5. Is 6 models enough for H1's universality claim, or should we add a reasoning-optimized model (o1, DeepSeek-R1) to stress-test the finding against models with explicit CoT?

## Risks being tracked

- **Discriminant validity fails (r > 0.8 sycophancy-bleed):** fallback positioning as "professional-framework sycophancy." Paper still stands.
- **Divergence matrix contested by domain experts:** narrow to only items with direct statute citations.
- **Liang et al. null (no internal role representation):** distinguish surface linguistic change from normative application; may limit H5's mechanistic claim to behavioral-only.
- **Model safety training sanitizes commitment-scenario cues:** may require adversarial prompt design.
- **30-hour budget overrun:** lit review + UI hit ~8 hours this week. Budget tighter for Week 2.

## Calibration executed (Thu 4/23, ~$0.70 spend)

Ran 5 calibration configurations on law domain (n=30, seed=42):

| Target | Observed | Published | Δ | Judge pair | Verdict |
|---|---|---|---|---|---|
| GPT-4o | 1.367 | 1.400 | −0.033 | GPT-4o + GPT-4o-mini | **PASS** |
| GPT-4o-mini | 1.467 | 1.400 | +0.067 | GPT-4o + GPT-4o-mini | **PASS** |
| Claude Sonnet 4.6 | 1.033 | (none) | — | Sonnet-4-6 + GPT-4o-mini | N/A |
| Claude Haiku 4.5 | 1.033 | (none) | — | Sonnet-4-6 + GPT-4o-mini | N/A |
| Claude Opus 4.5 | 1.033 | (none) | — | Sonnet-4-6 + GPT-4o-mini | N/A |

**Primary finding**: pipeline is calibrated against TRIDENT Figure 4 on both GPT models with |Δ| < 0.1. Confidence to proceed with novel scenarios is high.

**Anthropic note**: new-account restrictions gave us access only to Claude 4.x (no 3.5/3.7 snapshots), so strict TRIDENT replication on the Claude target side is not possible on this account. All 3 Claude 4.x models scored 1.033 — essentially all responses rated "1" (clean refusal). Inter-judge κ is 0 / Pearson r is NaN on two of these runs — that's the kappa paradox under uniform labels, not a scoring bug (93.3% and 100% exact agreement confirms).

Incidental issues resolved in-session:
- `httpx 0.28` dropped the `proxies` kwarg that `openai==1.54.4` still uses → pinned `httpx>=0.25,<0.28` in `pyproject.toml`.
- `/tmp/TRIDENT/dataset/` was wiped by macOS /tmp cleanup between sessions → moved dataset to `~/crosssystemeval/data/TRIDENT/dataset/` and updated `DEFAULT_DATASET_DIR` in `config.py`.
- Anthropic "credit balance too low" despite $20 funded → resolved by generating a fresh API key (root cause unclear; worth flagging if it recurs).
- matplotlib emitted `NaN` as non-strict JSON in `results.json` → added `_clean()` helper in `scripts/render_results.py`.
- `matplotlib` was missing from the venv → added it (not critical enough to pin in pyproject; users will install on demand).

UI updates:
- New `ResultsTable.tsx` component reads `ui/public/trident/results.json` and renders a 5-row table with PASS/FAIL verdict badges.
- TRIDENT page now shows the Apple-dark-themed bar chart PNG above the table.
- Chart is re-generated via `python scripts/render_results.py` (idempotent — just re-run after any new `trident-repro run`).

## Friday 4/24 — methodology cleanup + clean calibration

Issue raised (via review of Tue/Wed work): the original calibration drifted from the published research agenda in three ways: (a) jury composition swapped by target, (b) 3/5 runs had self-scoring (target model appeared in the jury), (c) κ and Pearson r were reported as judge-vs-judge agreement on the TRIDENT page but the agenda reserved those metric names for different constructs (κ_human on rubric annotation, r_H2 on ICR-vs-sycophancy discriminant).

### Model-availability drift from TRIDENT (documented)

TRIDENT's jury: Claude 3.5 Sonnet + Gemma 2-9B. Both inaccessible to us:

| Role | TRIDENT | Status | Fix |
|---|---|---|---|
| Judge A | Claude 3.5 Sonnet | Not accessible on new Anthropic accounts (tier-1 restriction) | Substituted with Claude Sonnet 4.6 (same vendor, same flagship tier) |
| Judge B | Gemma 2-9B | Decommissioned by Groq (error: `model_decommissioned`) | Substituted with Llama 3.1 8B Instant on Groq (similar 8B param class, also open-weight, also non-Anthropic/non-OpenAI) |

Adjacent TRIDENT **target** models also inaccessible from our infrastructure (Anthropic + OpenAI + Groq):
- Gemma 2-9B (as above)
- Mixtral-8x7b (removed from Groq)
- DeepSeek-R1-Distill-Llama 70B (not on Groq)
- Qwen3-235B (Groq has qwen3-32b only)
- All domain-specialized models (DISC-LawLLM, Saul-7B, FS-LLaMA, FinGPT, AdaptLLM-*, MedAlpaca, Meditron-7B/70B) — would require HF Inference Endpoints, Together.ai, Replicate, or self-hosting. Deferred to Week 3+ if H4 test is in scope.
- Claude 3.5 Sonnet as target (same issue as judge)

Implication: strict TRIDENT replication from our stack is architecturally blocked on both the jury and several target rows. Re-framed pipeline validation as **calibration** instead — documented explicitly in research agenda §5.4.

### Clean calibration executed (6 runs, ~$0.80 spend)

Standing jury: Claude Sonnet 4.6 (Judge A) + Llama 3.1 8B Instant via Groq (Judge B). No self-scoring (neither judge overlaps any GPT target).

| Target | Domain | n | Observed | Published | Δ | κ_judge | Pearson r_judge | Verdict |
|---|---|---|---|---|---|---|---|---|
| GPT-4o | law | 28 | 1.304 | 1.400 | −0.096 | 0.279 | 0.699 | **PASS** |
| GPT-4o | med | 26 | 1.115 | 1.400 | −0.285 | 0.000 | NaN | **PASS** |
| GPT-4o | finance | 25 | 1.320 | 1.400 | −0.080 | 0.242 | 0.945 | **PASS** |
| GPT-4o-mini | law | 30 | 1.467 | 1.400 | +0.067 | 0.464 | 0.939 | **PASS** |
| GPT-4o-mini | med | 25 | 1.360 | 1.400 | −0.040 | 0.226 | 0.356 | **PASS** |
| GPT-4o-mini | finance | 24 | 1.458 | 1.400 | +0.058 | 0.122 | 0.842 | **PASS** |

**Result**: **6/6 PASS** within ±0.3 tolerance. Pipeline calibrated across all three TRIDENT domains with substitute jury. Scoring trustworthy for novel Phase 1 scenarios.

Caveats disclosed on TRIDENT page:
- Partial runs (n ∈ {24..28} instead of 30) on 5 of 6 cells due to Groq's 30 req/min free-tier rate limit + our retry + partial-failure-resilience code. Not a bug — the pipeline kept the successful records rather than aborting. We could re-run with a paid Groq tier if tighter n=30 is required later.
- κ_judge is lower overall (0.00–0.46) than the original GPT-4o + GPT-4o-mini jury (0.75). Expected — Llama 3.1 8B is noisier than GPT-4o-mini as a judge. The substantive calibration metric (observed vs published mean) is what matters and passes cleanly everywhere.
- GPT-4o × med shows Δ = −0.285, right at the tolerance edge. Worth flagging but inside the pre-registered threshold.

### UI changes (labeling clarity)

- `κ` / `Pearson r` column headers in `ResultsTable.tsx` → `κ_judge` / `r_judge` (subscripted)
- New "Jury substitution trail" section on TRIDENT page documenting TRIDENT vs ours with rationale per role
- Caption under results table now disambiguates κ_judge from κ_human and r_H2
- Old self-scoring runs archived to `outputs/archive/` (not deleted — reproducibility record preserved)

### Research agenda update (§5.4)

Rewritten to:
1. Explicitly list jury substitutions with rationale
2. Frame the TRIDENT validation as **calibration, not replication** — with explicit reasons why strict replication is out of scope
3. Disambiguate the three uses of κ and r in the paper (judge-level, human-level, discriminant-level)

### Teaching moments captured

- **Inference** = running trained model weights on new input (distinct from training). Inference providers (Groq, Together, Fireworks) host open-weight models that the labs who trained them (Meta, Google, Mistral) don't serve directly — they specialize on inference hardware (Groq's LPUs at 500+ tok/s) and batching economics.
- **Scoring pipeline** = 9-stage chain: Loader → Sampler → Target client → Judge A/B clients → Parser → Jury aggregator → JSONL writer → Metrics → Reporter. Calibration validates the *whole chain* against an external reference, which unit tests alone cannot do.
- **Calibration vs. replication**: calibration validates the measuring stick against a known reference; replication reproduces the original findings. Calibration is sufficient when the research question is about *new* measurements, not reproducing old ones.

## Week 2 plan (4/27–5/3)

1. Build first NY scenario end-to-end: fact pattern, 6 role prompts, divergence matrix, rubrics with source citations.
2. Draft scoring schema (YAML per rubric item with statute/code references).
3. First dry run: 6 prompts × 3 primary models = 18 responses; manual smell test for bleed.
4. Bring one-pager to mentor meeting: scenario, matrix, 2-3 observed bleed examples.
5. Decide on Phase 1 jury composition: likely Claude Sonnet 4.6 + Llama 3.1 8B (standing) with per-target swaps where target == Claude Sonnet 4.6. Document in advance.
6. (Deferred to Week 3 if time) H4 test — 1 domain-specialized model (e.g. Meditron-7B) via HF Inference Endpoints on our **own** cross-system scenarios.
